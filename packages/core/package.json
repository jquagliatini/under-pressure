{
  "name": "@jqgl/under-pressure-core",
  "version": "0.0.1",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts",
    "!dist/**/*.spec.js",
    "!dist/**/*.spec.d.ts"
  ],
  "scripts": {
    "build": "tsc -p .",
    "test": "jest src",
    "doc": "typedoc --out docs --plugin typedoc-plugin-missing-exports src",
    "clean": "rimraf docs dist"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.5",
    "jest": "^29.7.0",
    "jest-mock-extended": "^3.0.5",
    "ts-jest": "^29.1.1",
    "typedoc": "^0.25.6",
    "typedoc-plugin-missing-exports": "^2.1.0"
  }
}
