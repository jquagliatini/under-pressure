{
  "name": "under-pressure-example",
  "version": "0.0.0",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "start": "node .",
    "build": "tsc -p .",
    "predev": "pnpm run build",
    "dev": "pnpm run start --watch",
    "build:watch": "pnpm run build --watch",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@jqgl/under-pressure-core": "workspace:*",
    "@jqgl/under-pressure-trackers": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.5"
  }
}
